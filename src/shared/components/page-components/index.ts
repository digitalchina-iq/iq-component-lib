import { PageErrorComponent } from "./page-error.component";
import { PageNotFoundComponent } from "./page-not-found.component";

export { PageErrorComponent, PageNotFoundComponent };

export let APP_ROUTING_COMPONENTS = [PageErrorComponent, PageNotFoundComponent];
